@model IEnumerable<WebProject.Models.Experience>

@{
    ViewBag.Title = "ViewExperiences";
}

@*<h2>ViewExperiences</h2>*@

<div class="module">
    <div class="module-head">
        <h3>Record</h3>
    </div>
    <div class="module-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Organization</th>
                    <th>Location</th>
                    <th>Postion</th>
                    <th>Start Date</th>
                    <th>Finish Date</th>
                    <th>Stipend</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.organization)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.location)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.position)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.startDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.endDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.stipend)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.details)
                        </td>
                    </tr>
                }

            </tbody>
        </table>

        <br />
        <!-- <hr /> -->
        <br />


    </div>
</div>

@*<table class="table">
    <tr>
        <th>
            Organization
        </th>
        <th>
            Location
        </th>
        <th>
            Position
        </th>
        <th>
            Details
        </th>
        <th>
            Start Date
        </th>
        <th>
            End Date
        </th>
        <th>
            Stipend
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.organization)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.details)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.startDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.endDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.stipend)
            </td>
        </tr>
    }

</table>*@
